rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {

        match /public/images/ {allPaths=**} {
            allow read, write: if true;
        }
    }
  }
}
